name: yeknom_ui_kit_example
description: Interactive catalog for the Yeknom Flutter UI kit.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=3.32.1"

dependencies:
  flutter:
    sdk: flutter
  yeknom_ui_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
