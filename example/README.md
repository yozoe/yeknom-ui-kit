# Yeknom UI Kit Catalog

这个示例应用用于独立检查 UI kit 的主题、语义色、响应式布局与公开控件。

## macOS

```bash
cd example
flutter pub get
flutter run -d macos
```

## Web

```bash
cd example
flutter run -d chrome
```

应用支持系统、浅色和深色三种模式，并在宽度低于 960px 时切换为紧凑导航。
