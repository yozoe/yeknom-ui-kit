name: yeknom_ui_kit
description: Shared visual language and reusable Flutter UI components for Yeknom desktop tools.
version: 0.1.0
publish_to: none
repository: https://github.com/yozoe/yeknom-ui-kit

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=3.32.1"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
